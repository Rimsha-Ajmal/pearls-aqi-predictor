name: Real-Time Data Pipeline (Hourly)

on:
  schedule:
    # Runs every hour
    - cron: "0 * * * *"
  workflow_dispatch:  # allows manual triggering

jobs:
  fetch_realtime:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install "hopsworks[python]==4.2.*" pyarrow pandas requests

      - name: Fetch real-time data
        env:
          OPENWEATHER_API_KEY: ${{ secrets.OPENWEATHER_API_KEY }}
          HOPSWORKS_API_KEY: ${{ secrets.HOPSWORKS_API_KEY }}
        run: |
          echo "🚀 Fetching real-time data..."
          python scripts/fetch_realtime_data.py
          echo "✅ Real-time data saved to raw_observations FG in Hopsworks."
